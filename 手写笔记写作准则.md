# 手写笔记写作准则（geluodan 版）

> 目标：写"经得起面试官追问"的实战笔记。
> 核心原则：**只写自己真实做过、真实想过的事。AI 可以帮忙排版，但不能替你思考。**

---

## 一、写作总纲：一篇文章 = 一个真实问题的完整解决过程

不要写"知识点整理"，要写"我遇到了什么问题 → 我怎么想的 → 我怎么解决的 → 我学到了什么"。

**判断标准：如果这篇文章删掉后，你还能在面试时完整复述它的细节 → 合格。**
（AI 写的文章删掉后你复述不出来，因为你没经历过。）

---

## 二、四段式结构（每篇都按这个骨架）

### 1. 背景与目标（1~2 段）
写清楚：
- 我面对的是什么目标/问题（靶机？SRC 站点？某漏洞？）
- 我当时的初始信息（只知道域名？只知道 IP？）
- 我期望得到什么结果

> 准则：**如实写"我一开始知道什么"**，不要事后诸葛。面试官喜欢看真实起点。

### 2. 过程与决策（核心，占 60%）
按时间顺序写，但**每一步都要回答"为什么"**：

| 写什么 | 怎么写才合格 |
|---|---|
| 我做了什么 | 具体命令/操作，但不止命令 |
| **我为什么这么做** | 当时看到了什么线索，促使我选这条路 |
| **我卡在哪里** | 卡住的点如实写（这是最有价值的部分） |
| **我怎么换思路的** | 什么线索让我意识到要换方向 |
| 我放弃了什么 | 为什么跳过某个点（展示判断力） |

> 准则：**"卡住"和"放弃"必须写**。AI 生成的文章没有卡顿，真人笔记一定有。面试官看到"我在这里卡了很久，后来发现……"反而更信。

### 3. 结论与验证（10%）
- 最终结果（flag / 漏洞确认 / 判断安全）
- 怎么验证的（截图/响应/复现）

### 4. 复盘（20%，决定文章上限）
三个固定问题：
1. **如果我重来一次，哪里能更快？**（下次的改进）
2. **如果我是防御方，怎么防住我这条路？**（攻防转换）
3. **这个经验还能用在什么场景？**（知识迁移）

> 准则：**复盘必须是你自己想的**，哪怕只有 3 句话，比 AI 写的 500 字"关键教训"值钱。

---

## 三、具体写作红线（违反=不合格）

### ❌ 禁止
1. **编造细节**：没做过的事不写（"我发现""我验证"必须是真事）；
2. **只贴命令不解释**：每一段操作后面必须跟"为什么"；
3. **事后诸葛**：不要假装一开始就知道答案；
4. **复制 AI 的总结**：你可以让 AI 检查语法，但不能让 AI 生成"关键教训"；
5. **写没验证过的内容**：写完自己复现一遍再发。

### ✅ 必须
1. 每个"我做了 X"后面跟"因为 Y"；
2. 每篇至少一个"卡住/踩坑"的真实片段；
3. 命令、输出、截图必须是你自己跑出来的（不是 AI 编的）；
4. 文末写"完成时间 + 我当时的环境"（Kali 版本、靶机 IP 等，真实痕迹）。

---

## 四、语言风格准则

1. **口语化可以，但术语要准确**——"打了个洞"可以，"这个洞是 SQLi"必须说对；
2. **第一人称"我"**：这是你的笔记，不是教科书；
3. **能短则短**：一句话能说清的别写三段。笔记的价值在"思考"，不在"字数"；
4. **中文为主**，命令/工具名/输出保留英文原样。

---

## 五、写完后自检清单（发博客前逐条打勾）

- [ ] 每个"我做了 X"都有"因为 Y"吗？
- [ ] 有真实记录"卡住/踩坑"吗？
- [ ] 所有命令/输出/截图是我自己跑的，不是 AI 编的？
- [ ] 复盘是我自己写的（不是 AI 总结）？
- [ ] 面试官挑任何细节问我，我都答得上吗？
- [ ] 脱敏了吗？（真实 SRC 不写目标域名/数据/未授权细节）

---

## 六、示例对比（同样是写 LFI 漏洞）

**❌ AI 风格（不许写这种）：**
> 使用 feroxbuster 扫描目录，发现 file.php 存在 LFI 漏洞，通过 `?file=/etc/passwd` 读取到 /etc/passwd，获取用户 tao。使用 hydra 爆破 SSH 密码 rockyou，登录后通过 sudo wfuzz 提权。

**✅ 手写风格（要写这种）：**
> 端口扫描只开了 22 和 80。首页标题写着 "Rockyou.txt - 密码字典文件介绍"，我当时觉得这提示太明显了，怀疑是误导，但也记下了。目录枚举时发现 file.php 返回 200 但页面空白——空白页面往往是动态加载，我第一个试的就是 file 参数，因为名字就叫 file。
> 读到 /etc/passwd 后我其实卡了一会儿：有 tao 这个用户，但首页一直暗示 rockyou，我犹豫要不要直接爆破 SSH（爆破有告警风险，靶机无所谓，但实战要考虑）。后来想想这是靶机，就上了 hydra，结果密码就是字典第一个词——**我猜对了，但这个"猜"的过程才是笔记该写的**。

> **差别在哪**：右边有"我当时怀疑""我犹豫了""我为什么这么选"——这些 AI 写不出来，而面试官只看这些。

---

## 七、最低起步标准

如果今天只写一篇，就写**今天真实的 SRC 侦察记录**（选目标 → crt.sh 通配符 → DNS 枚举 → 发现 wzzyedu.com → 指纹 → 内网 IP 泄露 → 判断搜索系统安全 → 止损）。

素材全是真实发生的，你只需要按四段式把它讲出来。写完发我，我帮你**检查结构**（只检查，不代写）。
